要求学生完成：

1.实现 sinusoidal position encoding； 
如task_1sinusoidal_pe.png所示。

2.实现二维向量旋转； 
如task2_2d_rotation_.png所示

3.实现高维 RoPE； 
已在代码中实现。

4.对比 E+pos 和 RoPE 的输入方式； 
一、位置注入时机
E+pos：在词嵌入运算结束、送入 Transformer 编码器整体输入前完成位置融合，整段序列一次性完成位置嵌入，后续所有网络层不再额外处理位置信息。
RoPE：不在模型输入阶段添加位置，在每一层多头注意力内部，Q、K 经过线性权重映射之后、注意力打分之前注入位置，每层注意力独立完成位置编码操作。
二、语义与位置融合形。
E+pos：采用向量加法融合，词嵌入向量与位置编码向量逐元素相加，语义信息和位置信息混杂在同一特征维度。
RoPE：采用矩阵旋转变换相乘，通过旋转矩阵分别对不同位置的 Q、K 特征做几何旋转，依靠乘法绑定位置，不直接修改原始语义向量数值。
三、Embedding 输入特征属性
E+pos：经过相加后的 Embedding 同时包含语义特征与绝对位置信息，不存在纯语义特征。
RoPE：原始词嵌入输出为纯净语义向量，全程不叠加任何位置编码，位置信息后续只附加在 Q、K 上。
四、QKV 特征携带位置情况
E+pos：输入统一融合位置，经过线性投影生成的 Q、K、V 三者全部附带绝对位置信息。
RoPE：仅 Q、K 特征经过旋转变换绑定位置编码，V 向量全程保持原始语义，不引入任何位置信息。
五、参数与存储开销
E+pos：可学习式 PE 需要占用最大序列长度×隐层维度的训练参数；正弦 PE 无训练参数，但需要提前预存固定位置编码表。
RoPE：无额外可学习参数，旋转角度依靠固定公式实时计算生成，不占用模型参数量与预存空间。
六、超长文本输入扩展性
E+pos：可学习 PE 受训练时预设最大长度限制，输入文本超出预设长度无法生成位置编码；正弦 PE 外推效果差，超长序列性能衰减明显。
RoPE：编码规则不绑定固定长度，旋转角度随位置动态计算，天然支持文本长度外推，适配超长上下文输入。
七、位置表达逻辑
E+pos：直接编码 token 的绝对序号位置，token 之间的相对位置关系需要模型通过数据自主学习拟合。
RoPE：基于绝对位置生成旋转参数，QK 内积运算结果天然由相对位置\(m-n\)决定，隐式实现相对位置建模。
八、输入代码逻辑简述
E+pos：词嵌入→获取全局位置编码→向量相加→合并特征送入 Transformer 整体前向传播。
RoPE：词嵌入（无位置）→QKV 线性映射→Q、K 单独旋转加位置→使用变换后的 QK 与原 V 计算注意力。

5.用数值实验验证 RoPE 的相对位置性质； 
如task5_relative_pos_verify.png所示

6.说明：为什么 RoPE 比简单的 E+pos 更巧妙？
位置嵌入时机更合理
E+pos 在最开头就把位置和词嵌入相加，整段特征永久混合位置，QKV 全都带位置；RoPE 只在注意力 Q、K 上加位置，V 保留纯语义，不破坏值特征。
数学天然自带相对位置
RoPE 利用旋转内积性质：结果只和位置差值有关，天然编码相对位置；E+pos 只存绝对位置，相对位置需要模型额外学习。
支持文本长度外推
E+pos（尤其是可学习 PE）受训练最大长度约束，超长输入失效；RoPE 依靠公式实时生成旋转角，不受预设长度限制，可推理更长文本。
无额外参数冗余
E+pos 的可学习位置编码占用大量参数；RoPE 无独立位置参数，依靠三角函数公式在线计算，轻量化。
位置干扰更小
E+pos 全程所有层特征都混入位置噪声；RoPE 仅在打分环节引入位置，对 FFN、中间特征干扰更少。

回答：

1. Transformer 为什么需要位置编码；
Transformer 依靠自注意力并行运算，舍弃了 RNN 时序循环结构，模型本身没有天然的时序先后感知能力，输入词向量打乱顺序后计算结果不变。
位置编码用于补充每个 Token 的序列先后信息，让模型区分词语排布顺序，学习语序、上下文依赖。

2. 传统 sinusoidal position encoding 是如何生成的；
采用正余弦交替生成固定位置向量，偶数维用正弦、奇数维用余弦，根据位置序号与维度索引实时计算，无需训练参数，预先构造位置表。

3. E + pos 的位置注入方式为什么有“内容和位置混合”的问题；
E+pos 在 Embedding 输出后直接执行X = Etoken + PEpos，词嵌入承载语义、位置编码承载位置，二者逐维度数值直接相加融合。
语义信息和位置信息揉在同一个特征维度中，特征无法拆分，后续 FFN、全层 QKV 全部被位置信息污染。

4. RoPE 不是加法，而是旋转；
RoPE 放弃加式融合，利用复数平面旋转变换嵌入位置：将特征两两分组视作复数，根据 token 位置生成对应旋转角，左乘旋转矩阵完成特征几何旋转，以乘法映射位置信息，保留原始语义向量主体结构，不直接叠加数值。

5. RoPE 作用在 Q 和 K 上；
经过W_Q,W_K,W_V得到 Q/K/V 后，只对 Q、K 特征施加旋转变换注入位置；V 向量全程保持原始嵌入映射结果，不附加任何位置信息，避免位置编码干扰数值特征的语义表达。

6. RoPE 的点积天然包含相对位置；
Q_m在m位置旋转、K_n在n位置旋转，二者内积化简后仅由位置差m-n决定，消除了m、n绝对位置的单独影响，不用额外参数即可编码相对距离。

7. attention score 里的相对位置关系可以通过旋转结构自然出现。
注意力分值由QKT内积得到，依托三角函数旋转的数学性质，旋转后的 Q、K 做内积运算时，相对位置关系被内嵌在注意力得分中，模型不需要额外学习相对位置特征。
